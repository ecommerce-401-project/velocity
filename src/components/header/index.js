import React from 'react';

import './style.css';
import Featured from '../featured';

const Header = () => {
  return (
    <div class="header">
      <Featured />
    </div>
  );
};

export default Header;
